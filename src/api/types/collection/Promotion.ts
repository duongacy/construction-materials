import type { Image } from '../common';

export type PromotionHave = {
  title: string;
  description: string;
  icon: Image;
};

export type PromotionNeed = {
  title: string;
  description: string;
  icon: Image;
};
