import type { Image } from '../common';

export type InvestmentHave = {
  title: string;
  description: string;
  icon: Image;
};

export type InvestmentNeed = {
  title: string;
  subTitle: string;
  description: string;
  icon: Image;
};
